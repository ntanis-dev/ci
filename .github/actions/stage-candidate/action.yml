name: Stage explicit Ntanis release files
description: Copies only declared top-level release outputs into the temporary transfer artifact.
inputs:
  source-directory:
    required: true
  include-files:
    required: true
  output-directory:
    required: true
runs:
  using: node24
  main: stage.mjs
